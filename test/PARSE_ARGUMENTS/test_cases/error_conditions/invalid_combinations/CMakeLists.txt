IF(NOT DEFINED CMAKE_SCRIPT_MODE_FILE)
	CMAKE_MINIMUM_REQUIRED(VERSION 3.18)
	PROJECT(CMLIB_PARSE_ARGUMENTS_INVALID_COMBINATIONS_FAIL_TEST)
ENDIF()

LIST(APPEND CMAKE_MODULE_PATH "${CMAKE_CURRENT_LIST_DIR}/../../../../../")
FIND_PACKAGE(CMLIB REQUIRED)

# This test should FAIL - invalid argument combinations
# Test with same argument name in multiple categories (if this causes error)
CMLIB_PARSE_ARGUMENTS(
	OPTIONS TEST_ARG
	ONE_VALUE TEST_ARG
	P_ARGN
		TEST_ARG "value"
)
