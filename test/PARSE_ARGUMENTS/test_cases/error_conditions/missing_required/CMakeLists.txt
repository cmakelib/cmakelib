IF(NOT DEFINED CMAKE_SCRIPT_MODE_FILE)
	CMAKE_MINIMUM_REQUIRED(VERSION 3.18)
	PROJECT(CMLIB_PARSE_ARGUMENTS_MISSING_REQUIRED_FAIL_TEST)
ENDIF()

LIST(APPEND CMAKE_MODULE_PATH "${CMAKE_CURRENT_LIST_DIR}/../../../../../")
FIND_PACKAGE(CMLIB REQUIRED)

# This test should FAIL - missing required arguments
CMLIB_PARSE_ARGUMENTS(
	ONE_VALUE TEST_VALUE
	REQUIRED TEST_VALUE
	P_ARGN
		# Intentionally not providing TEST_VALUE - this should cause FATAL_ERROR
)
